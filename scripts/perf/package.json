{
  "name": "howfar-perf-harness",
  "version": "0.0.0",
  "private": true,
  "description": "Mobile performance measurement harness for HowFar. Isolated from the app package.json on purpose — installing these deps must NOT change the app's dependency tree or lockfile. Run `npm install` inside scripts/perf/ to set it up.",
  "type": "module",
  "scripts": {
    "lighthouse": "node run-lighthouse.mjs",
    "profile": "node run-runtime-profile.mjs",
    "api": "node run-api-latency.mjs",
    "bundle": "node analyze-bundle.mjs",
    "all": "node run-all.mjs",
    "real": "bash measure-real.sh"
  },
  "dependencies": {
    "chrome-launcher": "^1.1.2",
    "lighthouse": "^12.8.2",
    "puppeteer-core": "^24.0.0"
  }
}
