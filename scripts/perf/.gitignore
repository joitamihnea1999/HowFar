# The perf harness installs its own deps, isolated from the app tree. Never commit them.
node_modules/
# Measurement outputs — regenerated each run; the committed record lives in docs/PERF_AUDIT.md.
results/
*.trace.json
*.report.json
*.report.html
